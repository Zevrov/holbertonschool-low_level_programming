#include "holberton.h"
/**
* add - adds together two integers a + b
* @a: the first integer
* @b: the second integer
* Return: returns the sum of a and b.
*/
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
