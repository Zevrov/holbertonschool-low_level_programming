#include "holberton.h"
/**
 * _atoi - convert string to numbers
 * @s: the string
 * Return: n
 */
int _atoi(char *s)
{
	/* int x; */
	/* char arr[]; */

	/* while (*s) */
	/* { */
	/* 	x++; */
	/* 	if (*s >= 48 && *s <= '9') */
	/* 		conv[i] = *s */
	/* } */

	return (0);
}
